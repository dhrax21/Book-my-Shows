name: Deploy BookMyShow API

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      # ✅ FIX 1: run Maven where pom.xml exists
      - name: Build Spring Boot JAR
        working-directory: Book_My_Show
        run: mvn clean package -DskipTests

      # ✅ FIX 2: build Docker image from correct directory on VM
      - name: Deploy to GCP VM via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_SSH_KEY }}
          script: |
            docker stop book-my-show || true
            docker rm book-my-show || true

            docker build -t book-my-show-api Book_My_Show

            docker run -d \
              -e SPRING_PROFILES_ACTIVE=prod \
              -e DB_URL=${{ secrets.DB_URL }} \
              -e DB_USER=${{ secrets.DB_USER }} \
              -e DB_PASSWORD=${{ secrets.DB_PASSWORD }} \
              -p 8080:8080 \
              --name book-my-show \
              book-my-show-api
